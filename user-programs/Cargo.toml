[package]
name = "user-programs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
path = "src/lib.rs"
test = false
doctest = false
bench = false

[dependencies]
riscv = "0.16.0"
sbi = "0.3.0"
